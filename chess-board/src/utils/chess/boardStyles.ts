import type { CSSProperties } from 'react'

export const LAST_MOVE_FROM: CSSProperties = {
  background: 'rgba(0, 0, 0, 0.2)',
}

export const LAST_MOVE_TO: CSSProperties = {
  background: 'rgba(0, 0, 0, 0.35)',
}
